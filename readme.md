# nkwen--traders--Beacon
This repository is for a group work which is for a business website.

## Updates & Instructions Implemented
- Edited `catalog.html` and `catalog.js` to dynamically fetch and render product data from `products.json`.
- Accurately mapped product descriptions and images to match the items in the JSON file.
- Enabled the price slider (the orange/yellow bar on the left) to filter products by maximum price dynamically.
- Extracted dynamic categories from `products.json` to replace the hardcoded category list in the sidebar.
- Implemented real-time search functionality in the sidebar to filter products by their names.
- Ensured the rest of the website layout and styling remained intact.
